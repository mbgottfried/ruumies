<div class="container white">
	<%= render 'layouts/sidebar' %>
	<div id="main" class="col-md-9">
		<h1 class="page-header"><%= current_user.house.name %> Dashboard</h1>
		<h3>Latest Updates</h3>
		<ul id="dashboard-bills">
		<% @house.bills.each do |bills| %>
			<li><%= bills.name %></li>
		<% end %>
		</ul>

	</div>
</div>