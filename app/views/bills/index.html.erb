<div class="container white">
  <%= render 'layouts/sidebar' %>
    <div class="col-md-9">
      <div class="page-header">
        <h1>Bills</h1><%= link_to 'New Bill', new_bill_path %>
      </div>
    <% @bills.each do |bill| %>
    Name: <%= bill.name %>
    <% end %>
  </div>
</div>

<%= link_to 'New Bill', new_bill_path %>
